{
  "name": "e-commerce",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo E-Commerce (Backend + Frontend)",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:be": "npm run dev --prefix backend",
    "dev:fe": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
